<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>ผลคะแนนสอบ</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/1.3.2/jspdf.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.3/jspdf.umd.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf-autotable/3.5.17/jspdf.plugin.autotable.min.js"></script>
</head>

<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    &nbsp;&nbsp;&nbsp;<a class="navbar-brand">Computer Programming</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <a href="/exammanage" class="btn btn-primary" style="margin-left:auto;">ย้อนกลับ</a>
    &nbsp;&nbsp;
    <a href="/logout" class="btn btn-danger">ออกจากระบบ</a>&nbsp;&nbsp;
  </nav>
  <div class="w3-container">
    <h1 class="text-center my-4">คะแนนสอบ</h1>
    <div class="container" style="margin-right:0px;">
      <button onclick="createExcel()" class="btn btn-warning">Export to Excel</button>
    </div>
    <br>
    <table class="table" style="height: auto; width: 1000px; margin-left: 200px;">
      <thead>
        <tr>
          <th scope="col">ลำดับ</th>
          <th scope="col">รหัสนักศึกษา</th>
          <th scope="col">ชื่อ</th>
          <th scope="col">นามสกุล</th>
          <th scope="col">คะแนนสอบ</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>1</td>
          <td>11623046</td>
          <td>วันจันทร์</td>
          <td>อังคาร</td>
          <td>20</td>
        </tr>
      </tbody>
    </table>
  </div>
</body>

</html>
<script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.16.9/xlsx.full.min.js"></script>
<script>
  function createExcel() {
    var table = document.querySelector("table");
    var ws_data = XLSX.utils.table_to_sheet(table);

    var range = XLSX.utils.decode_range(ws_data['!ref']);
    for (let C = range.s.c; C <= range.e.c; ++C) {
      var cell_address = { c: C, r: range.s.r };
      var cell_ref = XLSX.utils.encode_cell(cell_address);

      if (!ws_data[cell_ref]) continue; // ในกรณีที่ cell นั้นว่าง

      if (!ws_data[cell_ref].s) {
        ws_data[cell_ref].s = {};
      }
      ws_data[cell_ref].s.fill = {
        patternType: "solid",
        fgColor: { rgb: "FFFFA500" }
      };
    }

    var wb = XLSX.utils.book_new();
    XLSX.utils.book_append_sheet(wb, ws_data, "Sheet1");

    XLSX.writeFile(wb, "scores.xlsx");
  }
</script>